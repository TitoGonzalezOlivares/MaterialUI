import React from 'react';
import { Box, Grid, makeStyles} from '@material-ui/core';
import './Logo.css';

const Logo = (config) => {

    return (
        <div>
            <img src={'img/logo1.png'} className={'logo'}/>
        </div>
    )

}
export default Logo;